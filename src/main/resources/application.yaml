spring:
  application:
    name: devices-api

  datasource:
    url: jdbc:postgresql://localhost:5432/devicesdb
    username: devices
    password: AQalqlNM9
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      format_sql: true
    open-in-view: false
    show-sql: true

  jackson:
    serialization:
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info